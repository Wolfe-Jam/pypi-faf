[project]
name = "pypi-faf-build"
version = "0.1.0"
description = "Sovereign landing page for pypi.faf.one — auto-discovers FAF packages on PyPI"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27",
  "jinja2>=3.1",
  "pyyaml>=6.0",
]

[tool.uv]
package = false
